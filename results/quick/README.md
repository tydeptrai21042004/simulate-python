# Quick-run output

This directory was generated successfully by `configs/quick.yaml`.

The current run contains 24 rows (six methods × four scenarios). It uses one case, one seed, reduced input length and eight epochs on the bundled synthetic dataset. The files verify deterministic end-to-end execution only and are not final reproduction or scientific results.
